# MANDATE does not require an API key for the phase-one mock workflow.
# Add provider-specific keys here in later phases, and load them from env vars.
MANDATE_ENV=development
MANDATE_RULES_PATH=rules/authorization_rules.yaml
